# FTP server

Multithreaded FTP server for linux implemented in C++



#### compile and run
```
git clone https://github.com/Grigriy0/FTPServer
sudo apt install -y\
        libmysql++-dev\
        libmysqlclient-dev
make all

./Server <port>

```